<div class="modal-body">
  <h5 class="modal-title mb-3" id="expenseModalTitle"><%= expense.description %></h5>

  <div class="card bg-light mb-3">
    <div class="card-body d-flex align-items-center justify-content-between">
      <strong class="fs-3 text-primary"><%= number_to_currency(expense.amount, unit: "€", format: "%n %u") %></strong>
      <p class="text-muted mb-0">Expense amount</p>
    </div>
  </div>

  <div class="d-flex flex-column">
    <div class="mb-3">
      <label for="expense-category"><strong>Category:</strong></label>
      <p id="expense-category"> <%= expense.category %></p>
    </div>
    <div class="mb-3">
      <label for="expense-paid-by"><strong>Paid by:</strong></label>
      <p id="expense-paid-by"><%= expense.user.first_name %></p>
    </div>
    <div>
      <label for="expense-description"><strong>Full Description:</strong></label>
      <p id="expense-description"><%= expense.description %></p>
    </div>
  </div>
</div>
